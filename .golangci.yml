# For documentation, see https://golangci-lint.run/usage/configuration/

linters:
  enable:
    - unconvert
    - unparam
  disable:
    - typecheck
linters-settings:
  errcheck:
    exclude-functions:
      - (*log.Logger).Output
